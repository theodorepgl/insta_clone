<%= form_with model: @post do |f|%>
	
	<br>
	<%= f.label :caption%>
	<%= f.text_field :caption%>
	<br>
	<%= f.label :photos%>
	<%= f.file_field :photos, multiple: true%>

		<% if @post.new_record? %>
			<%= f.submit'Create' %>
		<% else %>
			<%= f.submit 'Update'%>
		<% end %>

<% end %>
